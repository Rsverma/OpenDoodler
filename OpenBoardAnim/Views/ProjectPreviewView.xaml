<UserControl x:Class="OpenBoardAnim.Views.ProjectPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OpenBoardAnim.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Canvas Margin="10" x:Name="PreviewCanvas">
            <Image x:Name="hand" Source="../resources/pencil.png" />
            <Path x:Name="AnimatedPath" Stroke="Black" StrokeThickness="2" />
        </Canvas>
        <Button Margin="0 10" Grid.Row="1" HorizontalAlignment="Center" Click="Button_Click">Play</Button>
    </Grid>
</UserControl>
