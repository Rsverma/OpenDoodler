<UserControl x:Class="OpenBoardAnim.Views.ProjectSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="600">
    <StackPanel Margin="30" Width="500" HorizontalAlignment="Center" VerticalAlignment="Top">
        <TextBlock Text="Project Settings" Style="{StaticResource TextBlockTitle}" Margin="0 0 0 20"/>
        <TextBlock Text="Background Music" Style="{StaticResource TextBlockSubTitle}" Margin="0 0 0 10"/>
        <StackPanel Orientation="Horizontal">
            <Button Click="BrowseAudio_Click">Choose...</Button>
            <Button Margin="10 0" Style="{StaticResource ButtonDanger}" Click="ClearAudio_Click" ToolTip="Remove Music" Content="{materialDesign:PackIcon Close}"/>
        </StackPanel>
        <TextBlock Margin="0 8 0 0" Text="{Binding AudioPath}" ToolTip="{Binding AudioPath}" TextTrimming="CharacterEllipsis" FontSize="12"/>
        <StackPanel Orientation="Horizontal" Margin="0 12 0 0">
            <TextBlock Text="Volume" VerticalAlignment="Center" Margin="0 0 8 0"/>
            <Slider Width="200" Minimum="0" Maximum="100" Value="{Binding AudioVolume, Mode=TwoWay}"/>
        </StackPanel>
    </StackPanel>
</UserControl>
